using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlDice : MonoBehaviour
{
    //private int sizeArr = 0;
    //// Start is called before the first frame update
    //void Start()
    //{
    //    newtestcrubb.start;
    //}
    //private void OnMouseDown()
    //{
    //    newtestcrubb.rend.sprite = newtestcrubb.dicesides[1];//to be in the begining of the game the num 2 n th dice side
    //    if (newtestcrubb.coroutineAllowed)
    //    {
    //        // StartCoroutine("RollTheDice");//אם המשחק לא נגמר  אז זורקים קוביה מפעלים פונקציה
    //        newtestcrubb.RollTheDice;

    //    }
    //    foreach (var x in results)//לראות את הlist
    //    {
    //        sizeArr++;
    //    }
    //    if (results.remove(sizeArr)=4|| results.remove(sizeArr) = 5 || results.remove(sizeArr) = 6)
    //    {
    //        newtestcrubb.coroutineAllowed = true;
    //    }

    //}
}