using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class addandremoveFromArr : MonoBehaviour
{
    public int inumberStep=0;
    public List<int> ArStepNums= new List<int>();
    // Start is called before the first frame update
    //void Start()
    //{
       
    //}

    // Update is called once per frame
    private void OnMouseDown()
    {
     //  newtestcrubb.ChooseNumSteps(ArStepNums, inumberStep);
      // newtestcrubb.DeletNumStepsFommArr(ArStepNums, inumberStep);
    }
}
