using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CubeArrayPosition : MonoBehaviour
{
    public List<GameObject> ArStepNums = new List<GameObject>();
    //Start is called before the first frame update
    //void Start()
    //{

    //}

    // Update is called once per frame
    private void OnMouseDown()
    {
        
        


    //CubeScript.ChooseNumSteps(CubeScript.results);
    //CubeScript.DeletNumStepsFommArr();
        
    }
}
